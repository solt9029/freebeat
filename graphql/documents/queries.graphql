query playlist($id: Int!) {
  playlist(id: $id) {
    id
    title
    defaultBpm
    videos {
      edges {
        node {
          id
          youtubeVideoId
          youtubeVideoTitle
          bpm
        }
      }
    }
  }
}
