query playlist($id: Int!) {
  playlist(id: $id) {
    id
    title
    defaultBpm
    maxPlaybackRate
    minPlaybackRate
    videos {
      edges {
        node {
          id
          youtubeVideoId
          youtubeVideoTitle
          bpm
        }
      }
    }
  }
}
